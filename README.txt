Abir Abbas
Family Tree Parser V3

Compile :

* must be in parser directory

make - compiles the shared library to the root directory!

To Run :

npm install
npm run dev (port number)

Important:

I have used one select file dropdown for multiple uses, Please select the file in the blue drop down
you wish to work on and the rest of the functionality will be dependent on your selected file!
